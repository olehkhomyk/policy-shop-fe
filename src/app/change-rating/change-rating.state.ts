import { Route } from '@angular/router/src/config';
import { ChangeRatingComponent } from './change-rating.component';

export const changeRating: Route = {
  path: 'change-rating',
  component: ChangeRatingComponent
};
